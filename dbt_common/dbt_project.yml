name: ee_common
version: 1.0.0
config-version: 2

profile: ee_common

restrict-access: true

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

models:
  ee_common:
    staging:
      +materialized: view
      +group: common
      +access: private
    marts:
      +materialized: table
      +group: common
      +access: public
