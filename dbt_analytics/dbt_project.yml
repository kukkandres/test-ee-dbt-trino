name: eesti_energia_analytics
version: 1.0.0
config-version: 2

profile: eesti_energia_analytics

restrict-access: true

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

models:
  eesti_energia_analytics:
    marts:
      +materialized: view
      +group: analytics
      +access: public
